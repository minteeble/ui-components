import {
  ClassExtensible,
  ParentComponent,
  StylableComponent,
} from "../../../models";

/**
 * InfoCardsStack interface props model
 */
export interface InfoCardsStackProps
  extends ClassExtensible,
    StylableComponent,
    ParentComponent {}
