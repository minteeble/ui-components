{
  "name": "@minteeble/ui-components-lib",
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "lerna exec \"rollup -c\"",
    "storybook": "lerna exec \"storybook dev -p 6006\"",
    "build-storybook": "lerna exec \"storybook build\"",
    "set-dev": "lerna run set-dev",
    "pub": "lerna run pub"
  },
  "workspaces": [
    "packages/*"
  ]
}
