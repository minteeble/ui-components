import React from "react";
import { TablePaginatorProps } from "./TablePaginator.types";

const TablePaginator = (props: TablePaginatorProps) => {
  return (
    <>
      <div className="table-paginator"></div>
    </>
  );
};

export default TablePaginator;
