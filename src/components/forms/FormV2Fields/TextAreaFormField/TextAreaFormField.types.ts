import { FieldComponentProps } from "../../FormV2/FormV2.types";

export enum TextAreaFormFieldResizeOption {
  None,
  Vertical,
  Horizontal,
  Both,
}

export interface TextAreaFormFieldProps extends FieldComponentProps {}
