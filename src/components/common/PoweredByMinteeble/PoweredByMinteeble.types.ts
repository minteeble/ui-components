/**
 * Props model for PoweredByMinteeble component
 */
export interface PoweredByMinteebleProps {
  /**
   * Specifies if the widget has to be a link to Minteeble website
   */
  link?: boolean;

  /**
   * Custom url to be used in the link
   */
  customUrl?: string;
}
